# bulkhead

Cross-process concurrency limiter (bulkhead) for PHP-FPM. Caps simultaneous
calls to a dependency across the whole worker pool via a shared Redis or APCu
counter.

Install:

```
composer require rasuvaeff/bulkhead
```

Runtime deps: `rasuvaeff/duration`. For `RedisBulkheadStore` (multi-host) also
install ONE Redis client — `predis/predis` ^2.2 (pure-PHP, `PredisScriptRunner`)
or `ext-redis` (`PhpRedisScriptRunner`); both are optional (`suggest`), not hard
dependencies. `ext-apcu` for `ApcuBulkheadStore` (single-host) — also optional.
Namespace `Rasuvaeff\Bulkhead`.

## Rules

- PHP 8.3+, `declare(strict_types=1)`, immutable `final readonly` classes.
- `lease` MUST exceed the longest expected callback runtime, or the slot is
  reclaimed mid-call and concurrency exceeds `maxConcurrent`. Rule of thumb:
  downstream timeout + margin.
- `maxWait` = `Duration::zero()` means fast-fail; otherwise the call polls up to
  `maxWait` (approximate, poll-based bound; default 50ms granularity).
- `pollJitter` (0.0..1.0, default 0.0) randomizes each poll sleep within
  ±(pollJitter × pollInterval) so waiters don't stampede a freed slot in lockstep.
- Waiting is NOT FIFO — under sustained overload a waiter can starve and be
  rejected while later arrivals get through.
- `name` must match `/^[A-Za-z0-9_.:-]+$/` (it becomes part of the Redis/APCu key).
- `InMemoryBulkheadStore` is single-process only (tests/CLI) — not a pool limiter.
- `ApcuBulkheadStore` only coordinates workers on the same host (APCu shared
  memory doesn't span machines) — use `RedisBulkheadStore` across hosts.
- The store is a hard dependency: if it's unreachable, `call()` fails closed
  (throws), it does not admit the call unboundedly. A Redis connection failure
  surfaces as `Predis\Connection\ConnectionException` (or the equivalent from
  your client / `\RuntimeException` from `PhpRedisScriptRunner`), not
  `BulkheadFullException` — catch both if you need to distinguish "full" from
  "store unreachable".
- `availableSlots()`/`activeCount()` on Redis WRITE (they prune expired members);
  don't point the store at a read-only replica.
- Redis acquire is atomic via one Lua script (prune expired -> check ZCARD <
  max -> ZADD; the key TTL never shrinks). Scripts go over EVALSHA with an EVAL
  fallback on NOSCRIPT. APCu has no server-side scripting, so `ApcuBulkheadStore`
  gets the same atomicity from a short-lived `apcu_add` spinlock around the
  read-check-write (spin budget configurable via `lockMaxAttempts`/`lockRetryMicros`).
- Implement `BulkheadScriptRunner` to back the Redis store with another client.

## API

```php
use Predis\Client;
use Rasuvaeff\Bulkhead\BulkheadFullException;
use Rasuvaeff\Bulkhead\Redis\PredisScriptRunner;
use Rasuvaeff\Bulkhead\RedisBulkheadStore;
use Rasuvaeff\Bulkhead\SharedBulkhead;
use Rasuvaeff\Duration\Duration;

$bulkhead = new SharedBulkhead(
    name: 'legacy-api',
    maxConcurrent: 10,
    store: new RedisBulkheadStore(new PredisScriptRunner(new Client(['host' => '127.0.0.1']))),
    lease: Duration::seconds(5),
    maxWait: Duration::millis(200),         // Duration::zero() = fast-fail
    // optional: pollInterval (Duration, default 50ms), pollJitter (float 0..1),
    // sleeper (SleeperInterface), onAccepted / onRejected
    // (Closure(string $name, Duration $waited): void)
);

try {
    $value = $bulkhead->call(static fn(): string => callDownstream());
} catch (BulkheadFullException $e) {
    // $e->name, $e->maxConcurrent — degrade gracefully
}

$bulkhead->availableSlots(); // int >= 0
$bulkhead->name();           // non-empty-string
$bulkhead->maxConcurrent();  // positive-int
```

With `ext-redis` instead of predis:

```php
use Rasuvaeff\Bulkhead\Redis\PhpRedisScriptRunner;

$redis = new \Redis();
$redis->connect('127.0.0.1');
$store = new RedisBulkheadStore(new PhpRedisScriptRunner($redis));
```

Single-host, no Redis:

```php
use Rasuvaeff\Bulkhead\ApcuBulkheadStore;

$bulkhead = new SharedBulkhead('svc', 10, new ApcuBulkheadStore(), Duration::seconds(5), Duration::millis(200));
```

In-process (tests / CLI), no Redis/APCu:

```php
use Rasuvaeff\Bulkhead\InMemoryBulkheadStore;

$bulkhead = new SharedBulkhead('svc', 2, new InMemoryBulkheadStore(), Duration::seconds(5), Duration::zero());
```

### BulkheadStore (implement for a custom backend)

```php
interface BulkheadStore
{
    // non-empty-string token on success, null when full
    public function tryAcquire(string $name, int $maxConcurrent, Duration $lease): ?string;
    public function release(string $name, string $token): void;
    public function activeCount(string $name): int;
}
```
